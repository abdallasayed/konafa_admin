name: konafa_admin
description: Admin Panel for Konafa App.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  image_picker: ^1.0.7
  http: ^1.2.0
  firebase_auth: ^4.17.4
  firebase_messaging: ^14.7.15

flutter:
  uses-material-design: true
